#ifndef SENSOR_H
#define SENSOR_H

#include <string>

#include "../osctypes.hpp"

namespace osc {
    class sensor {
        private:
        std::string id; // Unique string identifier for the component
        public:
    };
}

#endif // SENSOR_H